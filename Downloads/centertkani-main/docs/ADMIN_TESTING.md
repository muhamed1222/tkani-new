# План тестирования админ-панели

## ✅ Выполненные проверки

### 1. Проверка кода
- ✅ Нет критических ошибок линтера в админских компонентах
- ✅ Все компоненты правильно экспортированы
- ✅ Импорты корректны
- ✅ Исправлены предупреждения о зависимостях в useEffect

### 2. Структура компонентов
- ✅ `AdminProducts.jsx` - управление товарами с пагинацией и фильтрацией
- ✅ `AdminCategories.jsx` - управление категориями
- ✅ `AdminBrands.jsx` - управление брендами
- ✅ `AdminWorks.jsx` - управление работами
- ✅ `AdminOrders.jsx` - управление заказами
- ✅ `AdminUsers.jsx` - управление пользователями
- ✅ `AdminDashboard.jsx` - панель управления
- ✅ `Admin.jsx` - главный компонент с вкладками

### 3. API методы
- ✅ `adminAPI.getProducts(params)` - с поддержкой пагинации и фильтрации
- ✅ `adminAPI.createProduct`, `updateProduct`, `deleteProduct`
- ✅ `adminAPI.getCategories`, `createCategory`, `updateCategory`, `deleteCategory`
- ✅ `adminAPI.getBrands`, `createBrand`, `updateBrand`, `deleteBrand`
- ✅ `adminAPI.getWorks`, `createWork`, `updateWork`, `deleteWork`

---

## 📋 Чек-лист для ручного тестирования

### Авторизация
- [ ] Вход в админ-панель с ролью `admin`
- [ ] Проверка редиректа при отсутствии прав
- [ ] Проверка редиректа при отсутствии авторизации

### Управление товарами
- [ ] Загрузка списка товаров
- [ ] Поиск товаров (debounce 500ms)
- [ ] Фильтрация по категории
- [ ] Фильтрация по бренду
- [ ] Фильтрация по наличию (в наличии/нет в наличии/мало на складе)
- [ ] Пагинация (переключение страниц)
- [ ] Изменение количества товаров на странице (10/20/50/100)
- [ ] Создание нового товара со всеми полями
- [ ] Редактирование товара
- [ ] Удаление товара
- [ ] Загрузка изображений (главное и дополнительные)

### Управление категориями
- [ ] Загрузка списка категорий
- [ ] Создание категории (с автоматической генерацией slug)
- [ ] Создание категории с указанием slug
- [ ] Редактирование категории
- [ ] Удаление категории
- [ ] Проверка защиты от удаления категории с товарами (если реализовано на бэкенде)

### Управление брендами
- [ ] Загрузка списка брендов
- [ ] Создание бренда
- [ ] Редактирование бренда
- [ ] Удаление бренда
- [ ] Проверка защиты от удаления бренда с товарами (если реализовано на бэкенде)

### Управление работами
- [ ] Загрузка списка работ
- [ ] Создание работы с изображением
- [ ] Редактирование работы
- [ ] Удаление работы
- [ ] Загрузка изображений работ

### Управление заказами
- [ ] Загрузка списка заказов
- [ ] Фильтрация заказов по статусу
- [ ] Пагинация заказов
- [ ] Просмотр деталей заказа (все поля отображаются)
- [ ] Изменение статуса заказа
- [ ] Проверка отображения адреса доставки, телефона, способов доставки/оплаты

### Управление пользователями
- [ ] Загрузка списка пользователей
- [ ] Редактирование пользователя
- [ ] Изменение роли пользователя
- [ ] Удаление пользователя

### Панель управления (Dashboard)
- [ ] Загрузка статистики
- [ ] Отображение всех метрик
- [ ] Проверка актуальности данных

---

## 🔍 Проверка интеграции с бэкендом

### Требования к бэкенду
- [ ] Бэкенд запущен на `http://localhost:5001`
- [ ] API доступен по пути `/api/v1`
- [ ] Создан пользователь с ролью `admin`
- [ ] Созданы категории в БД (можно через `init_admin_data.py`)
- [ ] Настроена отдача статических файлов (`/static/products/`, `/static/works/`)

### Тестовые сценарии API

#### Товары
```bash
# Получить товары с пагинацией
GET /api/v1/admin/products?page=1&per_page=20

# Поиск товаров
GET /api/v1/admin/products?q=название

# Фильтрация
GET /api/v1/admin/products?category_id=1&brand_id=1&stock_status=in_stock

# Создать товар
POST /api/v1/admin/products (multipart/form-data)
```

#### Категории
```bash
# Получить категории
GET /api/v1/admin/categories

# Создать категорию
POST /api/v1/admin/categories
Body: {"name": "Новая категория", "slug": "novaya-kategoriya"}
```

#### Бренды
```bash
# Получить бренды
GET /api/v1/admin/brands

# Создать бренд
POST /api/v1/admin/brands
Body: {"name": "Новый бренд"}
```

#### Работы
```bash
# Получить работы
GET /api/v1/admin/works

# Создать работу
POST /api/v1/admin/works (multipart/form-data)
```

---

## ⚠️ Известные проблемы и ограничения

### Текущие ограничения
1. **Toast-уведомления**: В админ-панели используются только error state, нет toast-уведомлений об успешных операциях
2. **Множественная загрузка изображений**: Поле `images` в товарах поддерживается, но множественная загрузка файлов может требовать доработки на бэкенде
3. **Валидация**: Валидация данных выполняется на бэкенде, фронтенд показывает только общие ошибки

### Рекомендации по улучшению
1. Добавить toast-уведомления для успешных операций
2. Добавить подтверждение перед удалением (улучшить UX)
3. Добавить индикаторы загрузки при операциях
4. Улучшить обработку ошибок (более детальные сообщения)

---

## 🚀 Быстрый старт для тестирования

1. **Запустить бэкенд**:
   ```bash
   python3 app.py
   ```

2. **Запустить фронтенд**:
   ```bash
   npm run dev
   ```

3. **Войти в админ-панель**:
   - URL: `http://localhost:5173/admin`
   - Email: `admin@example.com` (или созданный через скрипт)
   - Password: `admin123` (или указанный при создании)

4. **Проверить основные функции**:
   - Переключение между вкладками
   - Загрузка данных
   - Создание/редактирование/удаление записей
   - Пагинация и фильтрация товаров

---

## 📝 Результаты тестирования

### Дата тестирования: ___________

### Функциональность
- [ ] Все основные функции работают
- [ ] Пагинация работает корректно
- [ ] Фильтрация работает корректно
- [ ] Поиск работает корректно
- [ ] CRUD операции работают для всех сущностей

### Производительность
- [ ] Загрузка данных происходит быстро
- [ ] Нет лишних запросов к API
- [ ] Пагинация улучшает производительность при большом количестве данных

### UX
- [ ] Интерфейс интуитивно понятен
- [ ] Ошибки отображаются понятно
- [ ] Формы валидируются корректно
- [ ] Модальные окна работают корректно

### Совместимость
- [ ] Работает в Chrome
- [ ] Работает в Firefox
- [ ] Работает в Safari
- [ ] Адаптивность на мобильных устройствах (если требуется)

---

## 🔧 Команды для отладки

### Проверка API
```bash
# Проверить авторизацию
curl -H "Authorization: Bearer <token>" http://localhost:5001/api/v1/auth/me

# Получить товары
curl -H "Authorization: Bearer <token>" http://localhost:5001/api/v1/admin/products?page=1&per_page=20

# Получить категории
curl -H "Authorization: Bearer <token>" http://localhost:5001/api/v1/admin/categories
```

### Проверка консоли браузера
- Открыть DevTools (F12)
- Проверить вкладку Console на наличие ошибок
- Проверить вкладку Network на корректность запросов

---

## ✅ Готово к тестированию

Все компоненты созданы, API методы реализованы, интеграция выполнена. Можно приступать к ручному тестированию.

