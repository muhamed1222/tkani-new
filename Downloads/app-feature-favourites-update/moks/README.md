# Моковые данные для "Тропа Нартов"

Эта папка содержит моковые данные для тестирования фронтенда приложения до готовности бэкенда.

## Структура файлов

### 📁 Основные файлы с данными

- **`users_mock.dart`** - Пользователи системы (5 пользователей)
- **`dictionaries_mock.dart`** - Справочники (типы, категории, районы, теги)
- **`places_mock.dart`** - Места и достопримечательности (10 мест + изображения + теги)
- **`routes_mock.dart`** - Туристические маршруты (6 маршрутов + остановки + изображения)
- **`reviews_mock.dart`** - Отзывы на места и маршруты (17 отзывов)
- **`favorites_mock.dart`** - Избранное и пройденные места/маршруты

### 📋 Главный файл

- **`mock_data.dart`** - Экспортирует все моковые данные для удобного импорта

## Как использовать

### Импорт всех данных:

```dart
import 'package:app/moks/mock_data.dart';

void main() {
  // Получить все места
  final places = mockPlaces;
  print('Всего мест: ${places.length}');
  
  // Получить все маршруты
  final routes = mockRoutes;
  print('Всего маршрутов: ${routes.length}');
  
  // Получить пользователей
  final users = mockUsers;
  print('Всего пользователей: ${users.length}');
}
```

### Импорт конкретного файла:

```dart
import 'package:app/moks/places_mock.dart';

void showPlaces() {
  for (var place in mockPlaces) {
    print('${place.name} - ${place.rating}⭐');
  }
}
```

## Содержимое данных

### 👥 Пользователи (5)
- Ахмед Карданов
- Мадина Бозиева
- Азамат Шогенов
- Зарема Кушхова
- Тимур Балкаров

### 📍 Места (10)
1. Чегемские водопады
2. Голубые озера
3. Национальный музей КБР
4. Канатная дорога на Эльбрус
5. Ресторан "Сосруко"
6. Отель "Sindika"
7. Памятник Нартам
8. Долина Нарзанов
9. Баксанское ущелье
10. Атажукинский сад

### 🗺️ Маршруты (6)
1. К подножию Эльбруса (120.5 км, авто)
2. Водопады и озера Чегема (45.8 км, комбинированный)
3. Исторический центр Нальчика (5.2 км, пеший)
4. Голубые озера и Черекское ущелье (68.3 км, авто)
5. Долина Нарзанов - треккинг (12.4 км, пеший)
6. Велотур по Атажукинскому саду (8.7 км, велосипедный)

### 🏷️ Справочники

**Типы мест (6):**
- Памятник
- Музей
- Природная достопримечательность
- Ресторан
- Отель
- Смотровая площадка

**Категории (5):**
- Экотуризм
- Агротуризм
- Культурный туризм
- Активный отдых
- Гастрономия

**Районы мест (6):**
- Нальчик
- Чегемский район
- Эльбрусский район
- Баксанский район
- Зольский район
- Черекский район

**Теги (12):**
Горы, Водопады, История, Культура, Природа, Семейный отдых, Фотогеничное, Доступно зимой, Эльбрус, Национальная кухня, Озера, Экстрим

### 💬 Отзывы
- 17 отзывов от разных пользователей
- Оценки от 4 до 5 звезд
- Отзывы на места и маршруты

### ⭐ Связи
- Избранные места и маршруты для каждого пользователя
- Пройденные места и маршруты
- Связи мест с тегами
- Связи маршрутов с тегами

## Примеры использования

### Получить место с изображениями:

```dart
import 'package:app/moks/places_mock.dart';

void getPlaceWithImages(int placeId) {
  final place = mockPlaces.firstWhere((p) => p.id == placeId);
  final images = mockPlaceImages.where((img) => img.placeId == placeId).toList();
  
  print('${place.name}');
  print('Изображений: ${images.length}');
  print('Рейтинг: ${place.rating}⭐');
}
```

### Получить маршрут с остановками:

```dart
import 'package:app/moks/routes_mock.dart';
import 'package:app/moks/places_mock.dart';

void getRouteWithStops(int routeId) {
  final route = mockRoutes.firstWhere((r) => r.id == routeId);
  final stops = mockRouteStops
      .where((s) => s.routeId == routeId)
      .toList()
      ..sort((a, b) => a.orderNum.compareTo(b.orderNum));
  
  print('Маршрут: ${route.name}');
  print('Длина: ${route.length} км');
  print('Остановок: ${stops.length}');
  
  for (var stop in stops) {
    final place = mockPlaces.firstWhere((p) => p.id == stop.placeId);
    print('${stop.orderNum}. ${place.name}');
  }
}
```

### Получить избранные места пользователя:

```dart
import 'package:app/moks/favorites_mock.dart';
import 'package:app/moks/places_mock.dart';

void getUserFavorites(int userId) {
  final favorites = mockFavoritePlaces
      .where((f) => f.userId == userId)
      .toList();
  
  print('Избранных мест: ${favorites.length}');
  
  for (var fav in favorites) {
    final place = mockPlaces.firstWhere((p) => p.id == fav.placeId);
    print('- ${place.name}');
  }
}
```

### Фильтрация мест по тегу:

```dart
import 'package:app/moks/places_mock.dart';
import 'package:app/moks/dictionaries_mock.dart';

void getPlacesByTag(int tagId) {
  final tag = mockTags.firstWhere((t) => t.id == tagId);
  final placeIds = mockPlaceTags
      .where((pt) => pt.tagId == tagId)
      .map((pt) => pt.placeId)
      .toSet();
  
  final places = mockPlaces
      .where((p) => placeIds.contains(p.id))
      .toList();
  
  print('Тег: ${tag.name}');
  print('Найдено мест: ${places.length}');
  
  for (var place in places) {
    print('- ${place.name} (${place.rating}⭐)');
  }
}
```

## Координаты

Все места содержат реальные координаты в Кабардино-Балкарии для корректного отображения на карте:

- Широта (latitude): ~43.2-43.5
- Долгота (longitude): ~42.4-43.6

## Изображения

Используются placeholder изображения из Unsplash. В продакшене замените на реальные фотографии достопримечательностей.

## Примечания

- Все данные являются тестовыми
- Контакты (телефоны, email) - вымышленные
- Рейтинги и отзывы сгенерированы для демонстрации
- Изображения - из бесплатных источников (Unsplash, Pravatar)

## Интеграция с бэкендом

При переходе на реальный бэкенд:

1. Замените импорты из `moks/` на API вызовы
2. Используйте те же модели из `models.dart`
3. Методы `fromJson()` уже готовы для парсинга ответов API
4. Структура данных полностью соответствует схеме БД

---

**Версия:** 1.0  
**Дата создания:** Октябрь 2024  
**Для проекта:** Тропа Нартов

