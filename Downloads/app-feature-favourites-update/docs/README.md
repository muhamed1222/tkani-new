# 📚 Документация проекта "Тропа Нартов"

Добро пожаловать в документацию Flutter-приложения **"Тропа Нартов"**!

## 📖 Содержание

### 🎨 Дизайн-система
- [**Руководство по дизайн-системе**](design-system/DESIGN_SYSTEM_GUIDE.md) - Полное руководство по использованию `AppDesignSystem` и `AppTextStyles`
  - Константы цветов, размеров, отступов
  - Готовые текстовые стили
  - Примеры использования
  - Правила и best practices

### 🧩 Виджеты
- [**Руководство по виджетам**](widgets/WIDGETS_GUIDE.md) - Переиспользуемые компоненты приложения
  - AppButton - универсальные кнопки
  - AppCard - карточки
  - AppInputField - поля ввода
  - AppDialog - диалоги
  - AppChip - чипы и теги
  - AppBadge - бейджи
  - AppProgressBar - прогресс-бары
  - AppSnackBar - уведомления

### 📋 Функциональность
- [**Руководство по Bottom Sheets**](features/BOTTOM_SHEETS_GUIDE.md) - Типы и использование bottom sheets
  - `openBottomSheet` - стандартный bottom sheet
  - `openBottomSheet50` - с темным фоном (opacity 0.9)
  - `openBottomSheetDynamic` - с динамическим затемнением
  - Когда использовать каждый тип
  - UX best practices

## 🚀 Быстрый старт

### Использование дизайн-системы

```dart
import 'package:tropanartov/core/constants/app_design_system.dart';
import 'package:tropanartov/core/constants/app_text_styles.dart';

// Текст с правильным стилем
Text(
  'Заголовок',
  style: AppTextStyles.title(),
)

// Контейнер с правильными константами
Container(
  decoration: BoxDecoration(
    color: AppDesignSystem.primaryColor,
    borderRadius: BorderRadius.circular(AppDesignSystem.borderRadius),
  ),
)
```

### Использование готовых виджетов

```dart
import 'package:tropanartov/core/widgets/widgets.dart';

// Кнопка
AppButton(
  text: 'Нажми меня',
  onPressed: () => print('Нажато'),
)

// Поле ввода
AppInputField(
  label: 'Email',
  hint: 'Введите email',
)

// Диалог
AppDialog.showConfirmDialog(
  context: context,
  title: 'Подтверждение',
  message: 'Вы уверены?',
)
```

## 📁 Структура проекта

```
lib/
├── config/              # Конфигурация приложения
├── core/
│   ├── constants/       # Константы (дизайн-система, стили)
│   ├── widgets/         # Переиспользуемые виджеты
│   ├── helpers/         # Вспомогательные функции
│   └── network/         # Сетевой слой
├── features/            # Основные фичи приложения
│   ├── home/           # Главный экран с картой
│   ├── places/         # Места
│   ├── routes/         # Маршруты
│   ├── profile/        # Профиль пользователя
│   ├── menu/           # Меню
│   └── respublic/      # Информация о республике
├── screens/            # Экраны (авторизация, онбординг)
├── services/           # Сервисы (API, авторизация)
├── models/             # Модели данных
└── utils/              # Утилиты

docs/                   # 📚 Документация (вы здесь)
├── design-system/      # Документация дизайн-системы
└── widgets/           # Документация виджетов
```

## ✅ Правила разработки

### 1. Всегда используйте дизайн-систему
- ✅ `AppTextStyles.body()` вместо `GoogleFonts.inter(fontSize: 16, ...)`
- ✅ `AppDesignSystem.primaryColor` вместо `Color(0xFF24A79C)`
- ✅ `AppDesignSystem.borderRadius` вместо жестких значений

### 2. Используйте готовые виджеты
- ✅ `AppButton` вместо `ElevatedButton` с кастомным стилем
- ✅ `AppInputField` вместо `TextFormField` с кастомным `InputDecoration`
- ✅ `AppCard` вместо `Container` с декорацией

### 3. Следуйте архитектуре
- Используйте Clean Architecture (Data → Domain → Presentation)
- Используйте BLoC для state management
- Разделяйте бизнес-логику и UI

### 4. Проверяйте код перед коммитом
```bash
# Найти жестко заданные значения
grep -r "fontSize:" lib --include="*.dart" | grep -v "AppDesignSystem\|AppTextStyles"
grep -r "Color(0x" lib --include="*.dart" | grep -v "AppDesignSystem"
```

## 🔧 Полезные команды

```bash
# Запуск приложения
flutter run

# Запуск на iOS симуляторе
flutter run -d "iPhone 15"

# Запуск на Android эмуляторе
flutter run -d emulator-5554

# Hot reload
r

# Hot restart
R

# Проверка зависимостей
flutter pub get

# Генерация иконок
flutter pub run flutter_launcher_icons

# Анализ кода
flutter analyze

# Форматирование кода
flutter format lib/
```

## 📝 Изменения и версии

### Версия 1.0 (12 ноября 2025)
- ✅ Реализована единая дизайн-система
- ✅ Созданы переиспользуемые виджеты
- ✅ Обновлены все экраны согласно Figma
- ✅ Миграция на GoogleFonts
- ✅ Добавлен `openBottomSheet50` для bottom sheets с темным фоном
- ✅ Добавлен `openBottomSheetDynamic` с динамическим затемнением
  - При раскрытии на 50% → средне затемненный фон (opacity 0.6)
  - При раскрытии на 100% → почти черный фон (opacity 0.9)
- ✅ Создана структурированная документация

## 📞 Контакты

Если у вас есть вопросы или предложения по улучшению документации:
- Создайте issue в репозитории
- Свяжитесь с командой разработки

---

**Последнее обновление:** 12 ноября 2025  
**Версия документации:** 1.0


