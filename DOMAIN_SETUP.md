# Настройка фронтенда для работы с доменом

## 🌐 Настройка переменных окружения

### Для разработки (`.env.development`)

```env
VITE_API_URL=http://localhost:5001/api
VITE_STRAPI_URL=http://localhost:1338
```

### Для production (`.env.production`)

```env
VITE_API_URL=https://api.yourdomain.com/api
VITE_STRAPI_URL=https://cms.yourdomain.com
```

## 📝 Шаги настройки

1. **Создайте файл `.env.production`** в корне проекта `tkani-new-main`

2. **Укажите ваши домены:**
   ```env
   VITE_API_URL=https://api.yourdomain.com/api
   VITE_STRAPI_URL=https://cms.yourdomain.com
   ```

3. **Соберите проект для production:**
   ```bash
   npm run build
   ```

4. **Результат будет в папке `dist/`**

## 🔧 Автоматическая настройка

Скрипт `setup_domain.py` в бэкенде также обновит инструкции для фронтенда.

## ⚠️ Важно

- Используйте `https://` для production
- Убедитесь, что CORS настроен на бэкенде для вашего домена
- Проверьте, что все API запросы идут на правильный домен


