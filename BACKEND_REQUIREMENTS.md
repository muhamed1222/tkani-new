# Требования к бэкенду для проекта "Центр Ткани"

## 🎉 СТАТУС: ВСЕ ТРЕБОВАНИЯ РЕАЛИЗОВАНЫ НА БЭКЕНДЕ!

Все endpoint'ы реализованы и готовы к использованию. Фронтенд интегрирован.

---

## ✅ Что уже подключено и работает

### 1. Работы (Works) - ✅ РАБОТАЕТ
- `GET /api/v1/works?page=1&limit=12` - получение списка работ с пагинацией
- Формат ответа: `{ works: [...], total: 12, page: 1, totalPages: 1 }`

### 2. Каталог товаров (Catalog) - ✅ ЧАСТИЧНО РАБОТАЕТ
- `GET /api/v1/catalog/products` - получение списка товаров
- `GET /api/v1/catalog/products/:id` - получение товара по ID (метод есть, но страница не реализована)
- `GET /api/v1/catalog/categories` - получение категорий

### 3. Аутентификация (Auth) - ✅ РАБОТАЕТ
- `POST /api/v1/auth/register` - регистрация (FormData с аватаром)
- `POST /api/v1/auth/login` - вход
- `GET /api/v1/auth/me` - получение текущего пользователя
- `PUT /api/v1/auth/update` - обновление профиля (FormData)
- `POST /api/v1/auth/change-password` - смена пароля

---

## ✅ Что реализовано на бэкенде (готово к использованию)

### 1. Восстановление пароля (Password Recovery) - ✅ РЕАЛИЗОВАНО

**Все endpoint'ы реализованы на бэкенде и подключены на фронтенде:**

#### 1.1. Отправка кода восстановления
- **Endpoint:** `POST /api/v1/auth/forgot-password`
- **Тело запроса:**
  ```json
  {
    "email": "user@example.com"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Код отправлен на вашу электронную почту"
  }
  ```
- **Используется в:** `src/pages/forgotPassword/ForgotPassword.jsx`

#### 1.2. Проверка кода подтверждения
- **Endpoint:** `POST /api/v1/auth/verify-code`
- **Тело запроса:**
  ```json
  {
    "email": "user@example.com",
    "code": "123456"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "token": "temporary_token_for_password_reset",
    "message": "Код подтвержден"
  }
  ```
- **Используется в:** `src/pages/verifyCode/VerifyCode.jsx`

#### 1.3. Повторная отправка кода
- **Endpoint:** `POST /api/v1/auth/resend-code`
- **Тело запроса:**
  ```json
  {
    "email": "user@example.com"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Код отправлен повторно"
  }
  ```
- **Используется в:** `src/pages/verifyCode/VerifyCode.jsx` (кнопка "Отправить код снова")

#### 1.4. Сброс пароля
- **Endpoint:** `POST /api/v1/auth/reset-password`
- **Тело запроса:**
  ```json
  {
    "email": "user@example.com",
    "code": "123456",
    "new_password": "newpassword123"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Пароль успешно изменен"
  }
  ```
- **Используется в:** `src/pages/resetPassword/ResetPassword.jsx`

---

### 2. Корзина (Cart) - ✅ РЕАЛИЗОВАНО

**Все endpoint'ы реализованы на бэкенде. API методы подключены в `src/http/api.js`:**

#### 2.1. Получение корзины
- **Endpoint:** `GET /api/v1/cart/`
- **Требует авторизации:** Да (Bearer token)
- **Ответ:**
  ```json
  {
    "success": true,
    "items": [
      {
        "id": 1,
        "product_id": 5,
        "product": {
          "id": 5,
          "title": "Двунитка Мокрый Асфальт",
          "price": 800,
          "image": "/image_wet_asphalt_1.png"
        },
        "quantity": 2.5,
        "total_price": 2000
      }
    ],
    "total": 2000,
    "total_items": 1
  }
  ```
- **Используется в:** `src/pages/basket/Basket.jsx` (нужно реализовать)

#### 2.2. Добавление товара в корзину
- **Endpoint:** `POST /api/v1/cart/add`
- **Требует авторизации:** Нет (работает через сессию/cookie или token)
- **Тело запроса:**
  ```json
  {
    "product_id": 5,
    "quantity": 2.5
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Товар добавлен в корзину",
    "cart_item": { ... }
  }
  ```

#### 2.3. Обновление количества товара
- **Endpoint:** `POST /api/v1/cart/update`
- **Тело запроса:**
  ```json
  {
    "product_id": 5,
    "quantity": 3.0
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Количество обновлено"
  }
  ```

#### 2.4. Удаление товара из корзины
- **Endpoint:** `POST /api/v1/cart/remove`
- **Тело запроса:**
  ```json
  {
    "product_id": 5
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Товар удален из корзины"
  }
  ```

#### 2.5. Очистка корзины
- **Endpoint:** `POST /api/v1/cart/clear`
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Корзина очищена"
  }
  ```

**✅ Реализовано:** Корзина работает через cookies (без авторизации), при авторизации привязывается к пользователю.

---

### 3. Заказы (Orders) - ✅ РЕАЛИЗОВАНО

**Все endpoint'ы реализованы на бэкенде. API методы подключены в `src/http/api.js`:**

#### 3.1. Создание заказа из корзины
- **Endpoint:** `POST /api/v1/orders/create`
- **Требует авторизации:** Да
- **Тело запроса (опционально):**
  ```json
  {
    "delivery_address": "г. Москва, ул. Примерная, д. 1",
    "phone": "+79991234567",
    "comment": "Комментарий к заказу"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "order": {
      "id": 1,
      "user_id": 5,
      "status": "pending",
      "total": 2000,
      "items": [...],
      "created_at": "2024-01-01T12:00:00Z"
    }
  }
  ```

#### 3.2. Получение списка заказов пользователя
- **Endpoint:** `GET /api/v1/orders/my`
- **Требует авторизации:** Да
- **Query параметры:**
  - `status` (опционально) - фильтр по статусу: `pending`, `processing`, `shipped`, `delivered`, `cancelled`
  - `page` (опционально) - номер страницы
  - `limit` (опционально) - количество на странице
- **Ответ:**
  ```json
  {
    "success": true,
    "orders": [
      {
        "id": 1,
        "status": "pending",
        "total": 2000,
        "items_count": 2,
        "created_at": "2024-01-01T12:00:00Z"
      }
    ],
    "total": 10,
    "page": 1,
    "totalPages": 1
  }
  ```
- **Используется в:** `src/pages/account/Account.jsx` (вкладка "Мои заказы")

#### 3.3. Получение деталей заказа
- **Endpoint:** `GET /api/v1/orders/:id`
- **Требует авторизации:** Да
- **Ответ:**
  ```json
  {
    "success": true,
    "order": {
      "id": 1,
      "user_id": 5,
      "status": "pending",
      "total": 2000,
      "delivery_address": "г. Москва, ул. Примерная, д. 1",
      "phone": "+79991234567",
      "comment": "Комментарий",
      "items": [
        {
          "id": 1,
          "product_id": 5,
          "product": {
            "id": 5,
            "title": "Двунитка Мокрый Асфальт",
            "price": 800,
            "image": "/image_wet_asphalt_1.png"
          },
          "quantity": 2.5,
          "price": 2000
        }
      ],
      "created_at": "2024-01-01T12:00:00Z",
      "updated_at": "2024-01-01T12:00:00Z"
    }
  }
  ```

#### 3.4. Обновление статуса заказа (для пользователя - отмена)
- **Endpoint:** `PUT /api/v1/orders/:id/status`
- **Требует авторизации:** Да
- **Тело запроса:**
  ```json
  {
    "status": "cancelled",
    "comment": "Причина отмены"
  }
  ```
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Статус заказа обновлен"
  }
  ```

**✅ Реализовано:** Все методы поддерживают пагинацию и фильтрацию. Используется в `src/pages/account/Account.jsx` (вкладки "Мои заказы" и "История заказов")

---

### 4. Страница товара (Product Detail) - ✅ РЕАЛИЗОВАНО

**Endpoint реализован на бэкенде. API метод подключен в `src/http/api.js`:**

#### 4.1. Получение детальной информации о товаре
- **Endpoint:** `GET /api/v1/catalog/products/:id`
- **Ответ должен содержать:**
  ```json
  {
    "success": true,
    "product": {
      "id": 1,
      "title": "Двунитка Мокрый Асфальт",
      "description": "Полное описание товара...",
      "price": 800,
      "image": "/image_wet_asphalt_1.png",
      "images": [
        "/image_wet_asphalt_1.png",
        "/image_wet_asphalt_2.png",
        "/image_wet_asphalt_3.png"
      ],
      "category_id": 1,
      "category": {
        "id": 1,
        "name": "Для дома"
      },
      "stock": 100,
      "rating": 5,
      "reviews_count": 10,
      "specifications": {
        "width": "150 см",
        "composition": "100% хлопок",
        "density": "180 г/м²"
      },
      "related_products": [
        {
          "id": 2,
          "title": "...",
          "price": 800,
          "image": "..."
        }
      ]
    }
  }
  ```

**✅ Реализовано:** Все дополнительные поля включены:
- `images` - массив изображений для галереи
- `specifications` - характеристики товара (JSON)
- `related_products` - похожие товары
- `reviews_count` - количество отзывов

---

### 5. Бренды (Brands) - ✅ РЕАЛИЗОВАНО

**Endpoint реализован на бэкенде. API метод подключен в `src/http/api.js`:**

#### 5.1. Получение списка брендов
- **Endpoint:** `GET /api/v1/catalog/brands`
- **Ответ:**
  ```json
  {
    "success": true,
    "brands": [
      {
        "id": 1,
        "name": "Египет",
        "slug": "egypt"
      },
      {
        "id": 2,
        "name": "Азия",
        "slug": "asia"
      }
    ]
  }
  ```

**✅ Реализовано:** Метод `fetchBrands()` обновлен и подключен к API.

---

### 6. Фильтрация и поиск товаров - ✅ РЕАЛИЗОВАНО

**Расширенная фильтрация реализована на бэкенде:**

#### 6.1. Расширенная фильтрация товаров
- **Endpoint:** `GET /api/v1/catalog/products`
- **Query параметры:**
  - `category_id` - фильтр по категории
  - `brand_id` - фильтр по бренду
  - `min_price` - минимальная цена
  - `max_price` - максимальная цена
  - `search` - поиск по названию/описанию
  - `sort` - сортировка: `price_asc`, `price_desc`, `name_asc`, `name_desc`, `newest`
  - `page` - номер страницы
  - `limit` - количество на странице
- **Ответ:**
  ```json
  {
    "success": true,
    "items": [...],
    "total": 50,
    "page": 1,
    "totalPages": 5,
    "filters": {
      "categories": [...],
      "brands": [...],
      "price_range": {
        "min": 100,
        "max": 5000
      }
    }
  }
  ```

**✅ Реализовано:** Все параметры фильтрации поддерживаются.

---

### 7. Обновление профиля - ✅ РЕАЛИЗОВАНО

**Метод реализован на бэкенде. Поддержка `phone` добавлена:**

#### 7.1. Обновление личных данных
- **Endpoint:** `PUT /api/v1/auth/update` (уже есть)
- **Нужно добавить поддержку:**
  - Обновление телефона (`phone`)
  - Загрузка аватара (уже поддерживается через FormData)

**✅ Реализовано:** Метод `updateProfile` поддерживает обновление всех полей, включая телефон.

---

### 8. Выход из системы (Logout) - ✅ РЕАЛИЗОВАНО

**Endpoint реализован на бэкенде. Метод подключен в `UserStore.jsx`:**

#### 8.1. Выход из системы
- **Endpoint:** `POST /api/v1/auth/logout`
- **Требует авторизации:** Да
- **Ответ:**
  ```json
  {
    "success": true,
    "message": "Вы успешно вышли из системы"
  }
  ```

**✅ Реализовано:** Метод `logout()` в `UserStore.jsx` обновлен и вызывает API.

---

## 📋 Статус реализации

### ✅ ВСЕ ЗАДАЧИ ВЫПОЛНЕНЫ:

1. ✅ **Восстановление пароля** - все 4 endpoint'а реализованы и подключены
2. ✅ **Корзина** - все 5 endpoint'ов реализованы и подключены
3. ✅ **Заказы** - все 4 endpoint'а реализованы и подключены
4. ✅ **Страница товара** - детальная информация с дополнительными полями
5. ✅ **Бренды** - endpoint реализован и подключен
6. ✅ **Расширенная фильтрация и поиск** - все параметры поддерживаются
7. ✅ **Обновление профиля** - доработано, поддержка всех полей
8. ✅ **Logout endpoint** - реализован и подключен

---

## 📝 Формат ответов API

Все ответы должны следовать единому формату:

### Успешный ответ:
```json
{
  "success": true,
  "data": { ... } // или напрямую поля, если это список
}
```

### Ошибка:
```json
{
  "success": false,
  "error": true,
  "message": "Описание ошибки"
}
```

### Ошибки валидации:
```json
{
  "success": false,
  "error": true,
  "message": "Ошибка валидации",
  "errors": {
    "email": ["Email обязателен"],
    "password": ["Пароль должен содержать минимум 6 символов"]
  }
}
```

---

## 🔐 Авторизация

- Используется Bearer token в заголовке `Authorization: Bearer <token>`
- Токен хранится в `localStorage` с ключом `authToken`
- API возвращает `access_token` (не `token`)

---

## 📦 Особенности

1. **Корзина без авторизации:** Корзина должна работать через сессию/cookie для неавторизованных пользователей
2. **Количество товара:** Поддерживается дробное количество (например, 2.5 метра)
3. **FormData:** Регистрация и обновление профиля используют FormData для загрузки аватара
4. **Пагинация:** Все списки должны поддерживать пагинацию с параметрами `page` и `limit`

---

## 🧪 Тестирование

После реализации каждого endpoint'а нужно протестировать:
1. Успешные запросы
2. Обработку ошибок (401, 403, 404, 500)
3. Валидацию данных
4. Работу с авторизацией (где требуется)

