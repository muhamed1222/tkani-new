# Анализ экрана "Традиционная культура и искусство"

## Общая информация
- **Файл:** `lib/features/respublic/presentation/widgets/culture_detail_widget.dart`
- **Виджет:** `CultureDetailWidget`
- **Тип:** Bottom Sheet (открывается через `DraggableScrollableSheet` в `_openCultureDetail`)
- **Открывается из:** `AboutRespublicWidget` при нажатии на карточку "Традиционная культура и искусство"

## Структура экрана

### 1. Заголовок и индикатор
```
┌─────────────────────────────────┐
│ [Индикатор перетаскивания]      │ ← 40x4px, цвет #BFBFBF
│                                 │
│ ↓ 26px (после индикатора)       │
│                                 │
│ [Заголовок "Традиционная        │ ← fontSize: 20, fontWeight: w600
│  культура и искусство"]          │
│                                 │
│ ↓ 28px (после заголовка)        │
│                                 │
└─────────────────────────────────┘
```

### 2. Кнопки переключения
```
┌─────────────────────────────────┐
│ [Кабардинцы] [Балкарцы]         │ ← Выбор между двумя народами
│                                 │   Высота: 44px, borderRadius: 12px
│                                 │   Фон: #F6F6F6
│                                 │   Активная кнопка: #24A79C
│                                 │
│ ↓ 16px (после кнопок)           │
│                                 │
└─────────────────────────────────┘
```

### 3. Контент (текст и изображения)
```
┌─────────────────────────────────┐
│ [RichText с выделением]         │ ← Текст с выделенными словами
│                                 │
│ ↓ 16px (между элементами)       │
│                                 │
│ [Изображение]                   │ ← 381x250px, borderRadius: 16px
│                                 │
│ ↓ 16px (между элементами)       │
│                                 │
│ [Обычный текст]                 │ ← Многострочный текст
│                                 │
└─────────────────────────────────┘
```

## Отступы и spacing

### Отступы контейнера:
- `padding: EdgeInsets.all(14)` - **Проблема:** Не соответствует стандарту
- Стандарт для bottom sheets: `EdgeInsets.only(left: 14, right: 14, top: 8)`

### Отступы внутри виджета:
- ✅ **После индикатора:** 26px (соответствует стандарту)
- ✅ **После заголовка:** 28px (соответствует стандарту)
- ✅ **После кнопок переключения:** 16px
- ✅ **Между элементами контента:** 16px

## Компоненты

### 1. Индикатор перетаскивания
- **Размер:** 40x4px
- **Цвет:** #BFBFBF
- **Радиус:** 3px
- ✅ Соответствует стандарту

### 2. Заголовок
- **Текст:** "Традиционная культура и искусство"
- **Стиль:** fontSize: 20, fontWeight: w600, color: #000000
- **Выравнивание:** Center
- ✅ Соответствует стандарту

### 3. Кнопки переключения
- **Контейнер:** SmoothContainer (height: 44px, borderRadius: 12px, color: #F6F6F6)
- **Кнопка "Кабардинцы":**
  - Использует `SmoothContainer` ✅
  - Активное состояние: цвет #24A79C, текст белый
  - Неактивное состояние: прозрачный фон, текст черный
- **Кнопка "Балкарцы":**
  - Использует `Container` с `BoxDecoration` ⚠️
  - Активное состояние: цвет #24A79C, текст белый
  - Неактивное состояние: прозрачный фон, текст черный
- **Проблема:** Несоответствие - одна кнопка использует `SmoothContainer`, другая `Container`

### 4. Текстовый контент
- **RichText:** Текст с выделенными словами (цвет #24A79C)
- **Обычный Text:** Многострочный текст
- **Стиль:** fontSize: 16, fontWeight: w400, height: 1.20
- ⚠️ **Проблема:** Контент не меняется при переключении между "Кабардинцы" и "Балкарцы"

### 5. Изображение
- **Размер:** 381x250px (жестко задано) ⚠️
- **Радиус скругления:** 16px
- **Фон:** #F6F6F6
- **Fit:** BoxFit.cover
- **Проблема:** Жестко заданная ширина не адаптируется к экрану

## Проблемы и рекомендации

### 🔴 Критические проблемы

1. **Двойной ClipPath**
   - Виджет обернут в `ClipPath` с `SmoothBorderClipper`
   - `DraggableScrollableSheet` в `_openCultureDetail` может также добавлять `ClipPath`
   - **Решение:** Убрать `ClipPath` из виджета, если он уже есть в `DraggableScrollableSheet`

2. **Несоответствие padding**
   - Используется `padding: EdgeInsets.all(14)`
   - Стандарт: `EdgeInsets.only(left: 14, right: 14, top: 8)`
   - **Решение:** Изменить на стандартный padding

3. **Несоответствие в кнопках переключения**
   - Кнопка "Кабардинцы" использует `SmoothContainer`
   - Кнопка "Балкарцы" использует `Container` с `BoxDecoration`
   - **Решение:** Использовать `SmoothContainer` для обеих кнопок

4. **Контент не меняется при переключении**
   - Кнопки переключают `_selectedButtonIndex`, но контент остается статичным
   - **Решение:** Добавить условный рендеринг контента в зависимости от `_selectedButtonIndex`

5. **Жестко заданная ширина изображения**
   - `width: 381` не адаптируется к экрану
   - **Решение:** Использовать `double.infinity` или `LayoutBuilder` для адаптивной ширины

### ⚠️ Потенциальные проблемы

6. **Отсутствие обработки ошибок изображения**
   - Нет `errorBuilder` для `Image.asset`
   - **Решение:** Добавить обработку ошибок

7. **Жестко заданная ширина кнопок**
   - `width: 188` в кнопках (хотя используется `Expanded`)
   - **Решение:** Убрать `width`, так как используется `Expanded`

8. **Отсутствие отступа снизу**
   - Нет отступа в конце контента
   - **Решение:** Добавить отступ снизу для лучшего UX

## Рекомендации по улучшению

### 1. Убрать двойной ClipPath
```dart
@override
Widget build(BuildContext context) {
  return Container(
    padding: const EdgeInsets.only(left: 14, right: 14, top: 8),
    color: Colors.white,
    child: SingleChildScrollView(
      // ...
    ),
  );
}
```

### 2. Исправить padding
```dart
Container(
  padding: const EdgeInsets.only(left: 14, right: 14, top: 8), // Стандартный padding
  color: Colors.white,
  // ...
)
```

### 3. Унифицировать кнопки переключения
```dart
// Обе кнопки должны использовать SmoothContainer
SmoothContainer(
  padding: const EdgeInsets.all(10),
  borderRadius: 12,
  color: _selectedButtonIndex == 1
      ? const Color(0xFF24A79C)
      : Colors.transparent,
  child: Center(
    child: Text(
      'Балкарцы',
      style: TextStyle(
        color: _selectedButtonIndex == 1
            ? Colors.white
            : const Color(0xFF000000),
        fontSize: 14,
        fontWeight: FontWeight.w500,
      ),
    ),
  ),
),
```

### 4. Добавить условный рендеринг контента
```dart
// В зависимости от _selectedButtonIndex показывать разный контент
_selectedButtonIndex == 0
    ? _buildKabardianContent()
    : _buildBalkarContent(),
```

### 5. Использовать адаптивную ширину изображения
```dart
SmoothContainer(
  width: double.infinity, // Вместо 381
  height: 250,
  borderRadius: 16,
  // ...
)
```

### 6. Добавить обработку ошибок изображения
```dart
Image.asset(
  'assets/people.png',
  width: double.infinity,
  height: double.infinity,
  fit: BoxFit.cover,
  errorBuilder: (context, error, stackTrace) {
    return Container(
      color: Colors.grey[200],
      child: const Center(
        child: Icon(
          Icons.image_not_supported,
          size: 48,
          color: Color(0xFF24A79C),
        ),
      ),
    );
  },
),
```

### 7. Убрать жестко заданную ширину кнопок
```dart
// Убрать width: 188, так как используется Expanded
Expanded(
  child: GestureDetector(
    onTap: () {
      setState(() {
        _selectedButtonIndex = 0;
      });
    },
    child: SmoothContainer(
      // Убрать width: 188
      padding: const EdgeInsets.all(10),
      // ...
    ),
  ),
),
```

## Положительные моменты

1. ✅ Правильные отступы после индикатора (26px) и заголовка (28px)
2. ✅ Используется `scrollController` для `SingleChildScrollView`
3. ✅ Есть состояние для переключения между народами (`_selectedButtonIndex`)
4. ✅ Используется `RichText` для выделения текста
5. ✅ Правильная структура с `SingleChildScrollView` для прокрутки контента

## Итоговая оценка

### Положительные стороны:
- ✅ Правильная структура отступов (26px, 28px)
- ✅ Используется scrollController
- ✅ Есть состояние для переключения
- ✅ Используется RichText для выделения

### Требует исправления:
- 🔴 Двойной ClipPath
- 🔴 Несоответствие padding
- 🔴 Несоответствие в кнопках (SmoothContainer vs Container)
- 🔴 Контент не меняется при переключении
- 🔴 Жестко заданная ширина изображения
- ⚠️ Отсутствие обработки ошибок изображения
- ⚠️ Жестко заданная ширина кнопок (хотя используется Expanded)

### Общая оценка: 6/10

Виджет имеет правильную базовую структуру, но требует исправления нескольких критических проблем, особенно связанных с переключением контента и адаптивностью.

