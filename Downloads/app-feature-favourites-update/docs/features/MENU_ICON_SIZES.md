# Размеры иконок на экране меню

Документация всех размеров иконок, используемых на экране меню (`lib/features/menu/presentation/pages/menu_page.dart`).

---

## 1. Иконка "Назад" (Back Icon)

**Расположение:** Верхняя часть экрана, слева в шапке

**Код:**
```dart
SvgPicture.asset(
  'assets/back.svg',
  width: MenuConstants.iconSizeSmall,   // 20.0
  height: MenuConstants.iconSizeSmall,  // 20.0
)
```

**Параметры:**
- **Размер иконки:** `20.0 × 20.0` (`MenuConstants.iconSizeSmall`)
- **Контейнер кнопки:** `40.0 × 40.0` (`MenuConstants.buttonIconSize`)
- **Скругление контейнера:** `8.0` (`MenuConstants.borderRadiusSmall`)
- **Цвет фона контейнера:** `#FFFFFF` (белый)

**Константы:**
- `iconSizeSmall = 20.0` → `AppDesignSystem.iconSizeSmall`
- `buttonIconSize = 40.0` → `AppDesignSystem.iconSizeLarge`

---

## 2. Иконки кнопок меню (_MenuButton)

**Расположение:** В кнопках меню (Личный кабинет, Обратная связь, О проекте, Настройки)

**Код:**
```dart
SvgPicture.asset(
  iconPath,  // 'assets/user2.svg', 'assets/support2.svg', 'assets/about_project.svg', 'assets/config.svg'
  width: MenuConstants.iconSizeSmall,   // 20.0
  height: MenuConstants.iconSizeSmall,  // 20.0
  colorFilter: ColorFilter.mode(
    MenuConstants.primaryColor,  // #24A79C
    BlendMode.srcIn,
  ),
)
SizedBox(width: 10),  // Отступ между иконкой и текстом
```

**Параметры:**
- **Размер иконки:** `20.0 × 20.0` (`MenuConstants.iconSizeSmall`)
- **Цвет иконки:** `#24A79C` (`MenuConstants.primaryColor`)
- **Отступ от текста:** `10.0` (жестко заданное значение)

**Применяется к:**
- `assets/user2.svg` - Личный кабинет
- `assets/support2.svg` - Обратная связь
- `assets/about_project.svg` - О проекте
- `assets/config.svg` - Настройки

**Константы:**
- `iconSize = 24.0` → `AppDesignSystem.iconSize`
- `spacingSmall = 8.0` → `AppDesignSystem.spacingSmall`

---

## 3. Изображение в блоке спонсорства

**Расположение:** В блоке спонсорства, справа

**Код:**
```dart
Image.asset(
  'assets/Cone.png',
  width: imageWidth,   // 40% от ширины экрана
  height: imageHeight, // Минимум 171.0 или высота контейнера
  fit: BoxFit.cover,
)
```

**Параметры:**
- **Ширина:** `40%` от ширины экрана (`screenWidth * 0.4`)
- **Высота:** Минимум `171.0` или высота контейнера (`MenuConstants.sponsorBlockMinHeight`)
- **Fit:** `BoxFit.cover`
- **Скругление:** `16.0` (`MenuConstants.borderRadiusLarge`)

**Константы:**
- `sponsorBlockMinHeight = 171.0` → `AppDesignSystem.cardMinHeight`
- `borderRadiusLarge = 16.0` → `AppDesignSystem.borderRadiusMedium`

---

## Сводная таблица размеров иконок

| Элемент | Размер иконки | Размер контейнера | Цвет | Отступ от текста | Константа |
|---------|---------------|-------------------|------|------------------|-----------|
| Иконка "Назад" | `20.0 × 20.0` | `40.0 × 40.0` | По умолчанию | - | `iconSizeSmall` |
| Иконки кнопок меню | `20.0 × 20.0` | - | `#24A79C` | `10.0` | `iconSizeSmall` |
| Изображение спонсорства | `40% × 171.0+` | - | - | - | Динамическое |

---

## Константы из MenuConstants

Все размеры используют константы из `MenuConstants`, которые ссылаются на `AppDesignSystem`:

```dart
// Размеры иконок
iconSize = 24.0          // AppDesignSystem.iconSize
iconSizeSmall = 20.0     // AppDesignSystem.iconSizeSmall
buttonIconSize = 40.0    // AppDesignSystem.iconSizeLarge

// Отступы
spacingSmall = 8.0       // AppDesignSystem.spacingSmall

// Скругления
borderRadiusSmall = 8.0  // AppDesignSystem.borderRadiusSmall
borderRadiusLarge = 16.0 // AppDesignSystem.borderRadiusMedium

// Высота блоков
sponsorBlockMinHeight = 171.0  // AppDesignSystem.cardMinHeight
```

---

## Визуальное представление

### Иконка "Назад":
```
┌─────────────────┐
│                 │
│   [←] 20×20     │  ← Контейнер 40×40
│                 │
└─────────────────┘
```

### Иконки кнопок меню:
```
┌─────────────────────────────────────┐
│  [🖼️] 20×20  Текст кнопки          │  ← Отступ 10px
│                                     │
└─────────────────────────────────────┘
  ↑
  Цвет: #24A79C
```

### Изображение спонсорства:
```
┌─────────────────────────────────────┐
│  Текст          │  [Изображение]    │
│                 │  40% ширины        │
│                 │  × 171px+         │
└─────────────────────────────────────┘
```

---

## Примечания

1. **Единообразие:** Все размеры используют единую дизайн-систему через `MenuConstants` и `AppDesignSystem`
2. **Иконки кнопок:** Все иконки в кнопках меню имеют одинаковый размер `20.0 × 20.0`
3. **Отступ:** Отступ между иконкой и текстом в кнопках меню составляет `10.0` пикселей (жестко заданное значение)
4. **Цвет иконок:** Иконки кнопок меню используют основной цвет приложения `#24A79C`
5. **Адаптивность:** Изображение спонсорства адаптивно и занимает 40% ширины экрана
6. **Минимальная высота:** Блок спонсорства имеет минимальную высоту `171.0`

