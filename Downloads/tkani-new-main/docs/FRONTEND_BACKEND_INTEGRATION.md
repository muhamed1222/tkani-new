# Интеграция фронтенда и бэкенда

## ✅ Что было сделано

### 1. Обновлен API клиент (`src/http/api.js`)
- ✅ Поддержка нового формата ответов с `success: true/false`
- ✅ Автоматическая обработка ошибок
- ✅ Поддержка FormData для регистрации (с аватаром)
- ✅ Обновлен базовый URL на `/api/v1/` (с обратной совместимостью)
- ✅ Добавлены методы для cart и orders API

### 2. Обновлены Stores
- ✅ **WorksStore**: обработка нового формата `{ works: [...], total, page, totalPages }`
- ✅ **UserStore**: работа с `access_token` вместо `token`, новый формат ответов
- ✅ **TkanStore**: интеграция с `/catalog/products` вместо `/tkans`

### 3. Добавлены новые API методы
- ✅ `catalogAPI` - для работы с каталогом товаров
- ✅ `cartAPI` - для работы с корзиной
- ✅ `ordersAPI` - для работы с заказами
- ✅ Обновлен `authAPI` - поддержка FormData для регистрации

### 4. Настроены переменные окружения
- ✅ Создан `.env` файл с `VITE_API_URL`
- ✅ Создан `.env.example` для примера

## 🔌 API Endpoints

### Работы (Works)
- `GET /api/v1/works?page=1&limit=12` - получить список работ

### Каталог (Catalog)
- `GET /api/v1/catalog/products` - получить список товаров
- `GET /api/v1/catalog/products/:id` - получить товар по ID
- `GET /api/v1/catalog/categories` - получить категории

### Аутентификация (Auth)
- `POST /api/v1/auth/register` - регистрация (FormData)
- `POST /api/v1/auth/login` - вход (JSON)
- `GET /api/v1/auth/me` - получить текущего пользователя
- `PUT /api/v1/auth/update` - обновить профиль (FormData)
- `POST /api/v1/auth/change-password` - изменить пароль

### Корзина (Cart)
- `GET /api/v1/cart/` - получить корзину
- `POST /api/v1/cart/add` - добавить товар
- `POST /api/v1/cart/update` - обновить количество
- `POST /api/v1/cart/remove` - удалить товар
- `POST /api/v1/cart/clear` - очистить корзину

### Заказы (Orders)
- `POST /api/v1/orders/create` - создать заказ
- `GET /api/v1/orders/my` - получить мои заказы
- `GET /api/v1/orders/:id` - получить заказ по ID
- `PUT /api/v1/orders/:id/status` - обновить статус заказа

## 📋 Формат ответов API

### Успешный ответ
```json
{
  "success": true,
  "works": [...],
  "total": 15,
  "page": 1,
  "totalPages": 2
}
```

### Ошибка
```json
{
  "error": true,
  "message": "Описание ошибки"
}
```

## 🔧 Настройка

### 1. Переменные окружения

Создайте файл `.env` в корне фронтенда:
```env
VITE_API_URL=http://localhost:5001/api/v1
```

### 2. Запуск

**Бэкенд:**
```bash
cd tkani-new-back
python3 app.py
```

**Фронтенд:**
```bash
cd tkani-new
npm run dev
```

## 🧪 Проверка интеграции

### 1. Проверьте, что бэкенд работает
```bash
curl http://localhost:5001/api/v1/catalog/categories
```

### 2. Проверьте, что фронтенд подключается
1. Откройте `http://localhost:5173`
2. Откройте DevTools (F12)
3. Перейдите на вкладку Network
4. Проверьте, что запросы идут на `http://localhost:5001/api/v1/`

### 3. Проверьте страницы
- `/our_works` - должна загружать работы из API
- `/catalog` - должна загружать товары из API
- `/auth` - должна работать авторизация
- `/registration` - должна работать регистрация

## 🔄 Обратная совместимость

API клиент автоматически обрабатывает:
- Старый формат ответов (без `success`)
- Новый формат ответов (с `success: true/false`)
- Старые URL (`/api/...`) - работают через обратную совместимость бэкенда

## ⚠️ Важные замечания

1. **Токены**: API возвращает `access_token`, а не `token`
2. **Регистрация**: Использует FormData (multipart/form-data) для поддержки аватара
3. **Формат данных**: Все ответы теперь содержат `success: true/false`
4. **Версионирование**: Используется `/api/v1/`, старые маршруты `/api/` также работают

## 🐛 Решение проблем

### CORS ошибка
Убедитесь, что в бэкенде настроен CORS:
```python
CORS(app, supports_credentials=True)
```

### 404 ошибка
Проверьте, что:
1. Бэкенд запущен на порту 5001
2. URL в `.env` правильный
3. Используется правильная версия API (`/api/v1/`)

### Пустые данные
1. Проверьте, что в базе данных есть данные
2. Запустите `python3 seed_works.py` для добавления тестовых данных
3. Проверьте консоль браузера на ошибки

